<% @products.each do |product| %>
	<%= link_to product_path(product.id), class: 'product', 'data-turbo-frame': '_top' do #para que se salga del frame de turbo %> 
		<%= image_tag product.photo, width: 100 if product.photo.attached? %>
	  	<h2><%= product.titel %></h2>
	  	<span><%= product.price %>$</span>
	<% end -%>
<% end -%>	